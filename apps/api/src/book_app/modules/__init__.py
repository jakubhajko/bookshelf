"""Domain modules: auth, users, books, shelves, interactions, search, recommendations (spec §4.1).

Only ``books`` exists so far (Phase 2, catalog only). Others are added
starting Phase 3 — see docs/implementation/plan.md.
"""

from __future__ import annotations

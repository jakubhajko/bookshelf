"""Catalog: books and their taxonomy (authors, genres, shelf tags, source similarities).

No API routes yet — book detail (``GET /books/{book_id}``) is Phase 4. Phase
2 is the data layer only: models, migrations, and the import adapter.
"""

from __future__ import annotations

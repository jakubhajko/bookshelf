"""CLI commands (spec §4.1, §11): import_catalog, seed_demo, build_popularity,
export_training_data, cleanup_sessions. Only import_catalog exists so far
(Phase 2) — the others land with the phases that need them.
"""

from __future__ import annotations
